const mocha = require('mocha')
const expect = require('chai').expect
const app = require('../index')


describe('', ()=>{

    it('', ()=>{ // name to describe test

        expect(app).to.exist

    })

})