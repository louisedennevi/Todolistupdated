'use strict';

var mocha = require('mocha');
var expect = require('chai').expect;
var app = require('../index');

describe('', function () {

    it('', function () {
        // name to describe test

        expect(app).to.exist;
    });
});